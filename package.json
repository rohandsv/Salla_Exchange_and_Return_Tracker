{
  "name": "salla-returns-app",
  "private": true,
  "scripts": {
    "install:all": "npm i --prefix appsail && npm i --prefix web-client",
    "build:appsail": "npm run build --prefix appsail",
    "build:web": "npm run build --prefix web-client",

    "start": "npm start"
  }
}
